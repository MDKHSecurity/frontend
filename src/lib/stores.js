import { writable } from "svelte/store";

// export const userDataStore = writable(null);
export const videosDataStore = writable([]);
export const quizzesDataStore = writable([]);
export const coursesDataStore = writable([]);
export const questionsDataStore = writable([]);